run example:

```
python -u node_class_new.py --dataset cora --dev 0
```

```
for dataset in cora pubmed citeseer texas wisconsin cornell
do
  python -u node_class_new.py \
  --dataset $dataset \
  --dev 0
done
```

